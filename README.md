# Copy unique media files to an output directory eliminating duplicates
Enumerate a folder and copy all unique images and videos to the destination folder, using exif data to organise images.

usage:
python3 copyunique.py [source_folder] [destination_folder]
